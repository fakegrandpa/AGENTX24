==============================================================================
AGENTX24 - HACKATHON PROMPT PACK
Workspace: D:\AGENTX24   |   One continuously evolving project   |   Git is LOCAL only
==============================================================================

HOW TO USE THIS FILE
- Each section contains ONE prompt between the ">>> COPY FROM HERE" and
  "<<< COPY TO HERE" markers. Copy everything between those markers.
- Replace every [PASTE ... HERE] placeholder before sending.
- The TOOL line tells you where to paste it.
- Never start a new project folder. Every prompt targets D:\AGENTX24 itself.

BUILD DOCUMENT SYSTEM (read once, then it is automatic)
- There is NO single mutable BUILD.md.
- Hour 0 creates BUILD1.md: the frozen architecture and planning record.
- Each new 3-hour requirement creates the NEXT number: BUILD2.md, BUILD3.md, ...
- Numbering rule: list existing BUILD*.md, take the highest number, add one.
  Never overwrite an existing one.
- Planning content is frozen once written. Only the CURRENT stage's document may
  be appended to, under its "Stage Outcome" heading.
- Source of truth order: (1) the actual codebase, (2) the newest BUILD[n].md,
  (3) earlier BUILD[n].md files, (4) Git history.
- README.md is the only always-current document for how to run the project.
- BUILD-LAST.md is a final summary, created near the end only. Not during stages.


==============================================================================
TOMORROW FLOW - CHEAT SHEET
==============================================================================

HOUR 0:
  OpenCode     -> Section 1  Main Problem Analysis -> BUILD1.md
  Antigravity  -> Section 2  Build MVP -> Verify -> Git Commit
  Antigravity  -> Section 7  Submission Check

EVERY NEW 3-HOUR FEATURE:
  OpenCode     -> Section 3  Analyze Feature -> creates BUILD[next].md + plan
  Antigravity  -> Section 4  Implement -> Verify
  Either tool  -> Section 5  Regression Test
  Either tool  -> Section 9  Git Checkpoint
  Antigravity  -> Section 7  Submission Check

IF BROKEN:
  Either tool  -> Section 6  Emergency Recovery

LOST / UNSURE WHERE YOU ARE:
  Either tool  -> Section 10 Quick Status Check

FINAL HOURS:
  Antigravity  -> Section 8  Demo Polish
  Either tool  -> Section 5  Regression Test
  Antigravity  -> Section 7  Final Submission Check

TIMING PER 3-HOUR STAGE (from AGENTS.md P6):
  0:00-0:15 analyze  |  0:15-1:45 implement  |  1:45-2:10 regression
  2:10-2:25 polish (only if green)  |  2:25-2:40 submission  |  2:40-3:00 buffer
  FREEZE RULE: last 20 minutes of a stage = fixes, verification, submission only.


==============================================================================
SECTION 1 - HOUR 0: MAIN PROBLEM ANALYSIS
TOOL: OPENCODE
USE WHEN: The main problem statement has just been revealed. Nothing built yet.
==============================================================================

>>> COPY FROM HERE

You are working in the existing hackathon workspace D:\AGENTX24. This workspace
will hold ONE continuously evolving project for the whole 24-hour event. New
feature requirements will arrive every 3 hours and will be integrated into this
same project. Do not create a new project, a subproject folder, or a scaffold.

Do this first, in order:
1. Read AGENTS.md in the workspace root and follow it as binding rules,
   including the named protocols P1-P9.
2. List the skills in .agents/skills/ and read the hackathon-architect skill.
3. Load and follow the hackathon-architect skill for this task.
4. Confirm no BUILD1.md exists yet. If one does, stop and tell me - we are not at
   Hour 0 and a later stage belongs to a different prompt.

This is an ANALYSIS AND PLANNING task only. Create BUILD1.md. Do not create
application code. Run no scaffolding or generator commands and install no
dependencies. The only files you may create or edit are BUILD1.md and .gitignore
(only if something is genuinely missing from it).

Analyze the problem statement below and produce:

1. REQUIREMENTS, split into four explicit buckets:
   - MUST: explicitly demanded, or the deliverable is incomplete without it
   - SHOULD: stated as desirable or clearly implied
   - NICE: cheap and impressive, not required
   - OUT: tempting but unstated or expensive - excluded for now
   Quote the wording of the problem statement wherever it decides something.

2. ACTORS AND PRIMARY JOURNEY: who uses this, and the single path from
   trigger -> processing -> visible result, in one sentence.

3. DELIVERABLE FORM: what is actually submitted and demonstrated, and any hard
   constraints (platform, offline/online, provided data, mandated tools).

4. ASSUMPTIONS I SHOULD NOT MAKE: every point the statement leaves open that
   would change the design (data source and volume, persistence, single vs
   multi user, auth, real-time vs batch, accuracy bar, external services). For
   each, state the assumption you will proceed with and pick the option that is
   cheapest to reverse later. Flag only genuinely blocking ambiguity as a
   question for me.

5. LIKELY JUDGING PRIORITIES: only where the problem statement justifies the
   inference. Label each as an inference, not a fact. Skip anything you cannot
   support from the text.

6. STACK DECISION: the fastest appropriate stack for this deliverable, judged on
   fluency and available known-good scaffolds, time to a first running
   end-to-end flow, fit for the constraints, ability to absorb unknown features
   without re-platforming, and setup risk. For each significant choice record:
   decision - one-line reason - cheaper alternative rejected and why.
   Also decide now: exact run command(s), the weakest persistence option that
   satisfies MUST, and where configuration/secrets live.
   Reject unless a MUST requirement literally demands it: microservices, queues,
   containers, orchestration, multi-agent frameworks, vector stores, unrequested
   auth or roles or migrations or i18n, monorepos, plugin systems, and any stack
   chosen for novelty rather than delivery speed.

7. SMALLEST COMPLETE END-TO-END MVP: the thinnest vertical slice a judge can
   watch work from start to finish. If an item can be removed while the journey
   still demonstrates the solution, remove it. State one observable success
   sentence: "Given X, the user does Y, and Z is visibly produced."

8. TECHNICAL RISKS: for each, likelihood, impact, and a pre-decided fallback.
   The fallback matters more than the warning.

9. EXTENSION POINTS for unknown future requirements, bought only where nearly
   free: core logic callable independently of its trigger or UI, data shape in
   one place, thin display layer, domain-based naming. Do not propose plugin
   registries, event buses, or abstract interfaces on speculation.

10. PRIORITIZED IMPLEMENTATION PLAN: numbered steps in build order, each
    independently verifiable, with the first running end-to-end slice as early
    as possible.

11. WHAT SHOULD NOT BE BUILT YET: an explicit list. Be blunt.

Then create BUILD1.md in the workspace root - the frozen Hour 0 architecture
record - with exactly these sections:
  Problem & Primary Journey
  Scope: MUST / SHOULD / NICE / NOT YET
  Stack & Key Decisions (decision - reason - rejected alternative)
  How to Run (setup, run, verify commands as planned)
  Architecture
  Data Shapes
  Extension Seams
  Core Flow Test (the observable success sentence - the baseline regression
    check for the whole hackathon)
  Build Order
  Risks & Fallbacks
  Not To Be Built Yet
  Stage Outcome (leave as an empty heading; the MVP build appends here)

Keep BUILD1.md short enough to be read in full at the start of any stage. Once
written, its planning sections are frozen: later stages get their own numbered
documents (BUILD2.md, BUILD3.md, ...). Do not create a mutable BUILD.md and do
not create BUILD-LAST.md.

STOP after the analysis and BUILD1.md are complete. Do not begin implementation.
End your reply with the summary, the not-yet list, and any blocking question.

--- MAIN PROBLEM STATEMENT ---
[PASTE MAIN PROBLEM STATEMENT HERE]
--- END OF PROBLEM STATEMENT ---

<<< COPY TO HERE


==============================================================================
SECTION 2 - BUILD INITIAL MVP
TOOL: ANTIGRAVITY
USE WHEN: BUILD1.md exists and the architecture is approved. Nothing built yet.
==============================================================================

>>> COPY FROM HERE

You are working in the existing hackathon workspace D:\AGENTX24. This is ONE
continuously evolving project. Build directly inside this workspace root. Do not
create a nested project folder, and do not delete or modify AGENTS.md,
.agents/skills/, .gitignore, or the Git configuration.

Do this first, in order:
1. Read AGENTS.md and follow it as binding rules, including protocols P1-P9.
2. Read BUILD1.md and the current workspace. BUILD1.md is the frozen Hour 0
   architecture record: Scope, Stack & Key Decisions, How to Run, Core Flow Test,
   Build Order.
3. List the skills in .agents/skills/ and read the mvp-builder skill.
4. Load and follow the mvp-builder skill for this task.
5. Inspect the current workspace contents before creating anything, and preserve
   everything already present.

Your task: implement the approved MVP from BUILD1.md.

Rules:
- Follow the architecture and stack in BUILD1.md. Deviate only for a concrete
  technical reason - for example the toolchain is unavailable or a dependency
  cannot install here. If you deviate, say so explicitly, explain why, and record
  it in the Stage Outcome section (see below) rather than editing the plan.
- Confirm the required toolchain is actually available by running version
  commands before scaffolding.
- Build ONE complete end-to-end vertical slice: data shape -> core operation ->
  trigger/entry point -> visible output -> wired together. A finished narrow path
  beats several half-built components.
- Implement only the MUST items from BUILD1.md Scope. Do not add features that
  are not in the plan. Working functionality outranks polish, styling, and
  animation at this stage.
- Keep the code simple and easy to modify: small files, honest names, existing
  conventions once they exist. No abstractions, no config systems, no premature
  layers.
- Use reliable well-known libraries for solved problems rather than hand-rolling.
  Pin dependency versions.
- Verify the empty scaffold runs before writing feature code.
- Handle the failure paths the demo will realistically hit (empty input, missing
  config, failed external call) with a clear message instead of a crash. Skip
  exhaustive validation.
- Confirm .gitignore covers dependency directories, virtual environments, build
  output, caches, local databases, and environment/secret files. Never commit
  secrets. If configuration is needed, add an example env file with placeholder
  values only.

Then verify for real (protocol P3). Do not claim anything works because code was
written:
1. Run the build / compile / typecheck / import path and get it clean.
2. Actually start the application or execute the entry point.
3. Walk the BUILD1.md Core Flow Test yourself and observe the expected result.
4. Fix every error you find with targeted changes. If the same fix fails twice,
   stop and re-diagnose the root cause instead of trying variants.

Then finish:
- Do NOT rewrite BUILD1.md's planning sections. APPEND to its "Stage Outcome"
  section instead: what was actually built, the run commands that really work
  and any drift from the planned ones, any deviation from the plan and why,
  deferred SHOULD/NICE items, and known limitations.
- Create or update README.md - the always-current run document - with a one-line
  description, setup, run command, and how to see the core flow work.
- Create a LOCAL Git commit checkpoint only after verification passed. Stage the
  intended paths, do not blindly add everything, and do not push anywhere.
  Message: "stage-0: working MVP - <what now works>"

Report at the end:
- Status: GREEN (builds, runs, core flow verified), YELLOW (runs but something is
  degraded or unverified - name it), or RED (does not run).
- Exactly what was implemented, file by file.
- The exact commands you ran and what you observed for each.
- What you tested and the result of each test.
- The commit hash and message.
- Known limitations and anything deliberately not built.

<<< COPY TO HERE


==============================================================================
SECTION 3 - NEW FEATURE ANALYSIS
TOOL: OPENCODE
USE WHEN: A new 3-hour requirement has just been announced.
==============================================================================

>>> COPY FROM HERE

A new feature requirement has been announced for the SAME existing hackathon
project in D:\AGENTX24. This is an addition to a living system that already
works. It is NOT a new project, not a rewrite, and not a reason to change the
stack or re-scaffold anything. The existing structure, stack, and conventions
are fixed inputs.

Do this first, in order:
1. Read AGENTS.md and follow it as binding rules, including protocols P1-P9.
2. Establish the build-document state: list all BUILD*.md files in the root, note
   the highest existing number N, and plan to create BUILD[N+1].md for this stage.
   Never overwrite an existing one.
3. Read BUILD1.md (original architecture, Scope, Core Flow Test) and the newest
   one or two stage documents (latest intent and their Stage Outcome blocks).
   Skim earlier ones only for decisions that bear on this requirement.
4. List the skills in .agents/skills/ and read the feature-integrator skill.
5. Load and follow the feature-integrator skill for this task.
6. Inspect the CURRENT codebase before concluding anything: list the tree, read
   the files likely involved, and search for the domain terms in the requirement.
   Related logic often already exists under a different name.

SOURCE OF TRUTH: the actual code, then the newest BUILD document, then earlier
BUILD documents, then Git history. Never assume an older document still describes
the implementation. Where a document and the code disagree, the code wins - note
the discrepancy in your report.

This is an ANALYSIS AND PLANNING task. Do NOT implement the feature and do not
edit application code. You WILL create this stage's BUILD[N+1].md.

Produce:

1. REQUIREMENT, PARSED LITERALLY:
   - Demanded: what the requirement explicitly says must exist, with key phrases
     quoted.
   - Acceptance test: one observable check - "Given X, do Y, see Z."
   - Boundaries: what it does NOT ask for. Put every adjacent temptation here.
   - Interpretation: if the wording is ambiguous, choose the reading that is
     cheapest to implement and easiest to extend, and state it. Raise it as a
     question to me only if the ambiguity genuinely changes the design.

2. WHAT ALREADY EXISTS: verified against the code, not the documents - including
   any partial support for this requirement, and the existing pattern for this
   class of change (how the last similar piece was added). Flag any place where a
   BUILD document no longer matches reality.

3. IMPACT MAP, in this exact form:
   Touch:   <file> - <what changes and why>
   Add:     <new file> - <why no existing file was the right home>
   Reuse:   <existing helper / component / pattern being reused>
   At risk: <existing behaviour that shares this code path>

4. REGRESSION RISKS: shared data shapes or stored data, shared entry points or
   state, function signatures with multiple callers, config or dependency
   changes, global styling or layout edits. For each, name the concrete existing
   behaviour that could break and how to check it.

5. SMALLEST RELIABLE INTEGRATION APPROACH: choose and justify one -
   add alongside (additive, nothing shared changes) / extend in place (parameter,
   branch, field, case) / extract once then reuse (only on the second real use) /
   localized reshape (smallest unit that unblocks it) / slice it (ship the
   smallest end-to-end version first). Additive is the default; a reshape needs a
   stated reason. Explicitly reject rebuilding, re-scaffolding, stack changes,
   speculative abstractions, and unrelated cleanup.

6. CONCRETE IMPLEMENTATION PLAN: numbered steps, each independently verifiable,
   naming the file and the nature of the change per step, in an order that keeps
   the project runnable between steps. Include the verification steps at the end:
   the acceptance test, the core flow test, and each at-risk item.

7. DEPENDENCY DECISION: state whether any new dependency is needed. Default is
   no. If yes, name it, pin an exact version, and justify the time it saves.

8. SCOPE CUT LINE: if implementation runs long, the minimum version that still
   satisfies the requirement, and what gets dropped first.

9. WHAT MUST REMAIN UNCHANGED: the working behaviour and files this stage must
   not disturb.

Then create BUILD[N+1].md in the workspace root - this stage's record - with
these sections:
  Requirement (exact wording as announced)
  Acceptance Test
  What Already Exists (verified against the code)
  Relevant Prior Context (decisions from earlier BUILD files that constrain this)
  Affected Files & Components (the Touch / Add / Reuse map)
  Integration Strategy (chosen option + why cheaper ones were rejected)
  Regression Risks (each with how it will be checked)
  Implementation Plan (numbered, each step independently verifiable)
  Architectural Decisions This Stage ("none" is a valid answer)
  Must Remain Unchanged
  Scope Cut Line
  Stage Outcome (leave as an empty heading; filled in after implementation)

Do not touch BUILD1.md or any earlier stage document. Do not create a mutable
BUILD.md and do not create BUILD-LAST.md.

End with the implementation plan formatted so I can hand it straight to another
agent for implementation, and tell me which BUILD number you created.

--- NEW FEATURE REQUIREMENT ---
[PASTE NEW FEATURE REQUIREMENT HERE]
--- END OF REQUIREMENT ---

<<< COPY TO HERE


==============================================================================
SECTION 4 - IMPLEMENT NEW FEATURE
TOOL: ANTIGRAVITY
USE WHEN: You have the implementation plan from Section 3.
==============================================================================

>>> COPY FROM HERE

Implement a new feature in the EXISTING hackathon project at D:\AGENTX24. This
is ONE continuously evolving project. The feature must be integrated into the
current codebase, and everything that already works must keep working. Do not
re-scaffold, do not change the stack, do not create a parallel project, and do
not modify AGENTS.md, .agents/skills/, or the Git configuration.

Do this first, in order:
1. Read AGENTS.md and follow it as binding rules, including protocols P1-P9.
2. Read the newest BUILD[number].md - the stage record created for this
   requirement - in full. Then read BUILD1.md for the original architecture and
   the Core Flow Test, and any earlier stage document the plan references.
3. List the skills in .agents/skills/ and read the feature-integrator skill.
4. Load and follow the feature-integrator skill for this task.
5. Inspect the ACTUAL current codebase and read every file the plan says you will
   touch before editing it. No edits to unread files. The code is the source of
   truth; if it contradicts a document, trust the code and say so.

Then implement the plan below.

Rules:
- Follow the plan. If you find it is wrong or incomplete, say so, describe the
  smaller or corrected approach, and proceed with the smallest reliable option -
  do not silently expand the work.
- Change only the files named in the plan's impact map. If a change outside that
  set turns out to be necessary, state why.
- Make the smallest reliable changes. Extend working code; do not rewrite it.
- Preserve all existing functionality. Never break old behaviour to make the new
  behaviour simpler without saying so explicitly.
- Follow the project's existing naming, layout, error handling, and styling
  conventions. Reuse existing helpers and components instead of adding parallel
  ones.
- Do not add features, screens, options, or refactors that the requirement did
  not ask for. Respect the stage document's "Must Remain Unchanged" list. If you
  spot an unrelated defect, note it for the Stage Outcome block and leave it alone
  unless it blocks this feature.
- Add a new dependency only if the plan calls for it. Pin the exact version.
- Work in small steps and keep the project runnable between them.
- If you are past roughly 60 percent of your time and the feature is not close,
  fall back to the plan's Scope Cut Line, record what was cut, and finish
  something that works.

Then verify for real (protocol P3). Do not claim success from written code:
1. Build / compile / typecheck cleanly.
2. Start the application or run the entry point cleanly.
3. Run the acceptance test for the new feature and observe the expected result.
4. Run the BUILD1.md Core Flow Test - it must still pass.
5. Test every item on the plan's Regression Risks and Must Remain Unchanged
   lists, including whether previously created or stored data still loads
   correctly.
6. Check logs and console for new errors.
7. Fix verified regressions with minimal targeted changes, then re-run steps
   1-4. If the same fix fails twice, stop and re-diagnose the root cause.

Then finish:
- APPEND to this stage's BUILD[number].md under "Stage Outcome": what was
  actually built, what was verified and how, what was cut or deferred, any
  deviation from the plan and why, and new known limitations. Leave the planning
  sections above it exactly as written.
- Update README.md if the run commands or the user-visible feature set changed.
- Do NOT edit, rename, or delete BUILD1.md or any earlier stage document. If an
  earlier document is now factually wrong about the code, append a one-line
  [correction] note there and mention it in your report.
- Create a LOCAL Git commit checkpoint only after verification passed. Stage the
  intended paths only. Do not push anywhere.
  Message: "stage-<N>: <feature> - <what now works>"

Report at the end:
- Status: GREEN / YELLOW (name what is degraded or unverified) / RED.
- Changed files, one line each, with what changed.
- Every test performed, the command or action used, and PASS/FAIL for each.
- Verified regressions found and how they were fixed.
- The commit hash and message.
- Anything cut, unfinished, or unverified.

--- IMPLEMENTATION PLAN ---
[PASTE OPENCODE IMPLEMENTATION PLAN HERE]
--- END OF PLAN ---

<<< COPY TO HERE


==============================================================================
SECTION 5 - REGRESSION TEST
TOOL: ANTIGRAVITY OR OPENCODE
USE WHEN: After any implementation, and before every submission or demo.
==============================================================================

>>> COPY FROM HERE

Verify the current state of the existing hackathon project at D:\AGENTX24. This
is ONE continuously evolving project with several stages of features already in
it. Your job is verification and minimal fixes only.

Do this first, in order:
1. Read AGENTS.md and follow it as binding rules, including protocols P1-P9.
2. Read the newest BUILD[number].md for this stage's Acceptance Test, Regression
   Risks, and Must Remain Unchanged list, plus BUILD1.md for the Core Flow Test.
   The BUILD documents give intended behaviour; the running code is the truth.
3. List the skills in .agents/skills/ and read the regression-guardian skill.
4. Load and follow the regression-guardian skill for this task.

Step 1 - Establish what actually changed, from the repository, not from memory:
  git status
  git diff --stat HEAD
  git diff HEAD -- <the interesting paths>
  git log --oneline -5
From the real diff, derive the blast radius: which user-visible behaviour do the
changed files serve, and what shared code (data shapes, stored data, helpers
with several callers, entry points, global styling, config, dependencies) was
touched. Everything that consumes touched shared code is in scope.

Step 2 - Test in risk-based tiers. This is a hackathon: prioritise high-risk,
high-visibility paths and stop when time runs out, but always finish Tier 1.

  TIER 1 - always:
    a. Build / compile / typecheck / import cleanly.
    b. Application starts or entry point executes without errors.
    c. The newest stage's Acceptance Test works.
    d. The BUILD1.md Core Flow Test works end to end.

  TIER 2 - the affected surface:
    e. Each existing behaviour that shares a code path with the recent changes.
    f. Data continuity: previously created or stored data still loads and renders.
    g. Earlier-stage features that consume a changed data shape or signature.
    h. Everything on the current stage's Must Remain Unchanged list.

  TIER 3 - only if time allows:
    i. Remaining earlier features not obviously affected - the BUILD[number].md
       series is a useful checklist of what should exist by now.
    j. Realistic demo failure inputs: empty state, missing config, invalid input,
       failed external call.
    k. A clean-copy run: copy or clone the committed state to a temporary folder,
       install from the lock file, and run it.

Do not spend time on exotic edge cases, load testing, or exhaustive input
fuzzing.

Step 3 - Classify every issue honestly:
  VERIFIED FAILURE - observed and reproducible. Fix now.
  SUSPECTED - reasoning suggests a problem but it was not reproduced. Spend up to
    two minutes reproducing it, otherwise report it as suspected. Never change
    code to fix a suspicion.
  COSMETIC / LOW - visible but harmless. Log it, do not fix it now.
Fix priority: does not build or run -> core flow broken -> newest feature broken
-> older feature broken -> cosmetic.

Step 4 - Fix minimally:
  Locate the cause in the actual diff first; most regressions are in the lines
  just written. Apply the smallest fix that resolves that one failure. Re-run the
  failing test, then re-run all of Tier 1. Do not refactor, do not rename, do not
  tidy, and do not rewrite working code for cleanliness. Do not build a large
  test suite - manual verification is faster here. If the same fix fails twice,
  state the root cause you now believe and switch approach or escalate.

Step 5 - Report exactly in this shape:

  STATUS: GREEN | YELLOW | RED

  TESTED
  - <test> -> <command or action> -> PASS/FAIL

  FAILED & FIXED
  - <failure> -> <root cause> -> <fix> -> re-tested: PASS

  FAILED & OPEN
  - <failure> -> <impact on the demo> -> <suggested next step>

  NOT TESTED (and why)
  - <area> - <not affected / no time / out of scope>

  KNOWN LIMITATIONS
  - <append these to the current stage's BUILD[number].md Stage Outcome>

  DOCUMENT DRIFT
  - <where a BUILD document no longer matches the code - report only, never
     rewrite history and never change working code to match an old plan>

If you made fixes and Tier 1 passes again, create a LOCAL Git commit. Do not
push anywhere. Message: "fix: <what was repaired>"

<<< COPY TO HERE


==============================================================================
SECTION 6 - EMERGENCY RECOVERY
TOOL: OPENCODE OR ANTIGRAVITY
USE WHEN: The project will not build, will not run, crashes, or a change broke
          something and you do not know what.
==============================================================================

>>> COPY FROM HERE

The hackathon project at D:\AGENTX24 is broken. Recover it. This is ONE
continuously evolving project under time pressure; getting back to a running
state outranks everything else. Git is local only - never push, and never run a
destructive Git command without asking me first.

Do this first, in order:
1. Read AGENTS.md and follow it as binding rules, including protocols P1-P9.
2. List the skills in .agents/skills/ and read the emergency-recovery skill.
3. Load and follow the emergency-recovery skill for this task.
Git history is the record of what changed; the newest BUILD[number].md says what
the change was trying to do. Use documents for intent only - the error and the
code are the evidence.

STEP 0 - STOP. Make no edits until steps 1-3 are done. Do not install, upgrade,
or remove dependencies as a guess. Do not clean anything up. Do not restart the
process repeatedly hoping it resolves. Every broken change is still on disk and
recoverable - keep it that way.

STEP 1 - Read the exact error. Capture the complete message: type, text, file,
line, and the top stack frames, on every relevant side of the system. Classify
the layer: install, compile/type, startup, runtime, integration/external,
data/stored-state, or environment (missing tool, wrong version, port in use,
wrong working directory). Read the file and line the error names. Reproduce it
deliberately once and record the exact command that reproduces it. Say so if it
is intermittent.

STEP 2 - Inspect the change history before editing anything:
  git status
  git diff --stat HEAD
  git diff HEAD -- <files named in the error>
  git log --oneline -10
Ask what is different between the last known working commit and now.
Uncommitted changes are the prime suspect. Also look for changes you did not
intend, such as rewritten config or lock files, and for work that strayed outside
the current stage document's planned diff surface.

STEP 3 - State ONE root cause hypothesis in the form: "X fails because Y,
introduced by Z." Check it against the evidence before touching code. If you
cannot name Y, gather one more piece of evidence instead of guessing - a targeted
log line, a minimal reproduction, or isolating the failing call. Prefer boring
explanations: typo, wrong import path, missing await, null value, changed
signature with a stale caller, uninstalled new dependency, stale build cache,
missing environment variable, wrong working directory, port conflict, version
drift.

STEP 4 - Apply the smallest safe fix for the identified cause. Change one thing,
then test. Never bundle several speculative changes. Do not rewrite the file, the
module, or the feature "to be safe". Do not rebuild the project from scratch. Do
not silence the error, wrap everything in a catch-all, or delete the failing call
while the cause stands.

STEP 5 - If the fix does not work after two attempts, stop patching. Identify the
last known working checkpoint from git log and explain the safest rollback
options to me, with the cost of each, and recommend one:
  - restore a single mangled file from the last good commit
  - shelve the broken uncommitted work with: git stash push -m "wip-broken"
  - branch from the last good commit and re-apply the feature minimally
  - reinstall dependencies from the lock file if the environment is corrupt
  - switch to the pre-decided fallback in BUILD1.md Risks & Fallbacks if an
    external dependency is failing
Do not run reset --hard, clean -f, force push, or branch deletion. Ask me before
any operation that discards work.

STEP 6 - Verify before claiming recovery:
  a. Build / compile / typecheck cleanly.
  b. Application starts or entry point runs cleanly.
  c. The BUILD1.md Core Flow Test works.
  d. The most recently added feature works, or is explicitly reported as
     reverted or degraded.
  e. No new errors introduced by the fix itself.

Then create a LOCAL Git commit: "fix: recover from <failure> - <cause>". Append
any shelved or reverted work to the current stage's BUILD[number].md Stage Outcome
so it is not forgotten. Do not rewrite a stage plan to match what you ended up
with, and do not delete a stage document because its feature was reverted.

Report:
  FAILURE: <the actual error>
  CAUSE:   <root cause, backed by evidence>
  ACTION:  <fix or rollback route taken>
  LOST:    <work discarded or reverted, if any>
  STATUS:  GREEN | YELLOW (name what is degraded)
  NEXT:    <how to safely re-apply anything that was reverted>

<<< COPY TO HERE


==============================================================================
SECTION 7 - PRE-SUBMISSION CHECK
TOOL: ANTIGRAVITY
USE WHEN: Before every stage submission, and for the final submission.
==============================================================================

>>> COPY FROM HERE

Prepare and verify a source-code submission for the hackathon project at
D:\AGENTX24. From now until I say otherwise, the code is frozen: no new
features, no refactors, no polish. Only submission-blocking fixes.

Do this first, in order:
1. Read AGENTS.md and follow it as binding rules, including protocols P1-P9.
2. Read README.md for the current run instructions, the newest BUILD[number].md
   for this stage's feature and known limitations, and BUILD1.md for the Core Flow
   Test. Documents are context; everything you certify must be observed in the
   running project.
3. List the skills in .agents/skills/ and read the submission-manager skill.
4. Load and follow the submission-manager skill for this task.

Hard constraints:
- Do NOT push to GitHub or any remote. Do NOT create a remote, do NOT upload
  anything, and do NOT change the Git configuration. Git stays local.
- Do NOT blindly delete files. If something looks like it should be removed, list
  it and ask me. The only exception is content that is already ignored by
  .gitignore and therefore not part of the repository anyway.
- Do NOT modify AGENTS.md or .agents/skills/.
- Do NOT edit, tidy, rename, or delete any BUILD[number].md file. They are
  historical records and must ship as-is.
- If the organizer requirements below conflict with anything here, the organizer
  requirements win. If they are empty or unclear on a point, use the defaults
  here and tell me what you assumed.

Do this:

1. WHAT STATE ARE WE SHIPPING:
   git status
   git log --oneline -5
   git diff --stat HEAD
   Identify uncommitted or untracked changes. If they are verified and belong in
   this submission, commit them locally; if they are unverified or broken,
   exclude them and say so. Confirm the newest verified work is actually
   committed and that HEAD is on the intended branch. Confirm the current status
   label: GREEN, YELLOW (name what is degraded), or RED.
   If the project is RED, say so immediately and recommend either recovery or
   submitting the last known good checkpoint - do not dress it up as fine.

2. DOES IT RUN FROM THE COMMITTED STATE:
   - Confirm every file needed to run is tracked (check git ls-files against the
     project tree), especially recently added source, assets, schemas, and config.
     Every BUILD[number].md must also be tracked.
   - Run the build / compile / typecheck and get it clean.
   - Start the application or run the entry point.
   - Walk the BUILD1.md Core Flow Test and observe it working.
   - Confirm the newest stage's Acceptance Test passes.
   - Confirm the run instructions in README.md match the commands that actually
     work, including any required environment variables. README.md is the
     current-state document; a stale "How to Run" inside BUILD1.md is expected and
     is not a defect.
   - If time allows, copy the committed state to a temporary folder, install from
     the lock file, and run it there. This catches missing files and hardcoded
     local paths.

3. HYGIENE - what must not ship:
   - Secrets: environment files, credential files, key files, tokens, private
     certificates. Scan the tracked file list for anything that looks like a
     secret. If a secret was ever committed, tell me plainly and treat the key as
     compromised. Do not attempt Git history rewriting.
   - Dependency directories, virtual environments, build output, caches, local
     databases, logs, temporary files, editor and OS metadata, and large media
     not needed to run. Verify these are ignored and not tracked - report them,
     do not delete them from disk.
   What must ship: all source, assets, schemas and config needed to run; lock and
   pin files; the dependency manifest; an example environment file with
   placeholder values documenting every required variable; README.md with setup
   and run steps; every BUILD[number].md; AGENTS.md; plus anything the organizers
   require.

4. PACKAGE - only if the organizer requirements call for an archive:
   - Commit locally and optionally tag the submission point, e.g.
     stage-<N>-submission.
   - Build the archive from a CLEAN COPY of the committed state, not from the
     working directory, so ignored files stay out.
   - Name it as the organizers specified, otherwise AGENTX24_stage<N>.zip.
   - Verify the archive: list its contents, check the size is plausible, and
     confirm no dependency directories, caches, or secrets are inside.
   - Tell me the exact local path of the package.

5. REPORT a PASS/FAIL checklist with real observed results. Mark anything you
   could not verify as FAIL or SKIPPED with the reason - never as PASS:

   SUBMISSION - Stage <N>          STATUS: GREEN | YELLOW | RED
   Commit: <hash> <message>   Branch/Tag: <branch> / <tag>

   [PASS/FAIL] Organizer format and naming requirements followed
   [PASS/FAIL] Latest verified work committed; nothing needed left uncommitted
   [PASS/FAIL] Build / compile passes from committed state - <command>
   [PASS/FAIL] Application starts / entry point runs - <command>
   [PASS/FAIL] Core flow observed working - <what was observed>
   [PASS/FAIL] Stage <N> feature observed working - <what was observed>
   [PASS/FAIL] Run instructions match reality
   [PASS/FAIL] All required files tracked, incl. every BUILD[number].md
               (clean-copy run: yes/no)
   [PASS/FAIL] Lock file, manifest and config included
   [PASS/FAIL] No secrets tracked
   [PASS/FAIL] No dependency directories, caches or build junk tracked
   [PASS/FAIL] No historical BUILD document was modified
   [PASS/FAIL] Archive or package verified - <local path, if applicable>

   EXCLUDED ON PURPOSE: <unverified or broken work left out>
   KNOWN LIMITATIONS: <from the newest Stage Outcome, verified against the app>
   ITEMS NEEDING MY DECISION: <anything you refused to delete or change>

--- ORGANIZER SUBMISSION REQUIREMENTS ---
[PASTE ORGANIZER SUBMISSION REQUIREMENTS HERE, IF ANY]
--- END OF ORGANIZER REQUIREMENTS ---

<<< COPY TO HERE


==============================================================================
SECTION 8 - DEMO POLISH
TOOL: ANTIGRAVITY
USE WHEN: The project is working (GREEN) and time remains before a demo.
==============================================================================

>>> COPY FROM HERE

Polish the hackathon project at D:\AGENTX24 for demonstration. This is ONE
continuously evolving working project.

Do this first, in order:
1. Read AGENTS.md and follow it as binding rules, including protocols P1-P9.
2. Read BUILD1.md for the Problem & Primary Journey and the Core Flow Test, and
   the newest BUILD[number].md to know which features exist and what must remain
   unchanged. Then judge the app by what it actually does when you run it, not by
   what any document claims.
3. List the skills in .agents/skills/ and read the demo-polisher skill.
4. Load and follow the demo-polisher skill for this task.

PRECONDITION - check this before changing anything: build the project, start it,
and confirm the core flow works. If the project does not build, does not run, or
the core flow fails, STOP and report it instead of polishing. Polish on a broken
project is wasted work; recovery comes first.

Then walk the primary journey as a first-time stranger would and list what hurts
comprehension: unclear purpose, hidden main action, placeholder or lorem text,
dead controls, debug output, misalignment, default framework branding, console
errors, operations that look frozen, errors that look like crashes, blank areas,
and anything that requires typing before the value is visible.

Rank the issues by visible impact divided by risk of breaking something, and fix
in that order. Do not attempt the whole list. Work in small increments and
re-check the app after each one.

Priorities, highest value first:
1. Frame the value: clear title, a one-line statement of what this does, and one
   obvious primary action.
2. Seed / sample data or a one-click example so the flow can be demonstrated
   instantly and reliably - only where it genuinely helps. Label it as sample
   data.
3. Empty states: replace blank areas with a short line saying what will appear
   and how to make it appear.
4. Loading states: any operation over roughly 300ms gets visible feedback.
5. Error states: plain-language message plus a recovery hint instead of raw
   stack traces or silent failure. Keep technical detail in the logs.
6. Consistency: unify spacing, alignment, typography scale, colours, control
   styles, and terminology.
7. Remove noise: debug prints, unused controls, unreachable views, half-built
   features that invite awkward questions.
8. Basic accessibility that also reads as quality: readable contrast, labelled
   controls, visible focus, sensible heading order, keyboard-usable main flow.
If the deliverable is not visual, apply the equivalents: helpful help output,
clear progress logging, readable success and error output, a sample input, and a
one-command demo path.

Hard limits:
- Do NOT rewrite, restructure, or "improve" core business logic, data shapes, or
  algorithms.
- Do NOT redesign the whole application, migrate to a component library, or
  introduce a theme system now.
- Do NOT add fake functionality: no mock data presented as real output, no
  buttons that do nothing, no claimed capabilities that do not exist, no
  simulated progress or invented scores. If the project genuinely uses AI or
  agent behaviour, make it visible and legible; if it does not, add nothing.
- Do NOT trade reliability for visual flair.
- Do NOT polish screens that are not on the demo path.
- Do NOT modify AGENTS.md or .agents/skills/.
- Do NOT edit earlier BUILD[number].md files.

Then verify: build cleanly, start the app, re-run the BUILD1.md Core Flow Test,
and check every screen or output you touched. Cosmetic edits break layouts and
bindings more often than expected - verify, do not assume.

Then APPEND a "Demo Script" block to the CURRENT (highest-numbered)
BUILD[number].md, inside or directly after its Stage Outcome section:
  Setup:      commands and prerequisites to have it running
  Reset:      how to return to a clean demo state
  Path:       numbered steps - action -> what the judge sees
  Talk track: the one-sentence value proposition
  Avoid:      known-fragile areas not to click during the demo
  Fallback:   what to do if a live step fails
If the polish changed how the app is started or presented, update README.md too.

If everything still works, create a LOCAL Git commit: "polish: <what improved>".
Do not push anywhere.

Report: status GREEN/YELLOW, what you changed and why, what you deliberately
left alone, what you verified, and the commit hash.

<<< COPY TO HERE


==============================================================================
SECTION 9 - QUICK GIT CHECKPOINT
TOOL: ANY
USE WHEN: Verified work needs saving and no other skill just committed it.
==============================================================================

>>> COPY FROM HERE

Create a local Git checkpoint in D:\AGENTX24. Read AGENTS.md protocols P4 and P5
first.

1. Run: git status, then git diff --stat HEAD, then git log --oneline -3
2. Review what actually changed and summarise it in one line for me.
3. Confirm nothing sensitive or generated is about to be staged: environment or
   credential files, keys, tokens, dependency directories, virtual environments,
   build output, caches, local databases, logs, or large binaries. If anything
   like that appears, stop and tell me instead of committing it.
4. Confirm the changes are verified working. If the project does not build or run,
   or the changes were never exercised, do NOT commit to this branch - say so and
   recommend stashing or a scratch branch instead.
5. Stage the intended paths explicitly. Do not blindly stage everything.
6. Commit with a message describing what actually changed and what now works,
   based on the real diff, not on intent. Use the existing message style from
   git log.
7. Run git status and git log --oneline -1 afterwards and confirm the working
   tree is clean and the commit landed.
8. Do NOT push. Do NOT create a remote. Do NOT tag unless I ask.

Report: the commit hash, the message, the files included, and anything you
deliberately left out.

<<< COPY TO HERE


==============================================================================
SECTION 10 - QUICK PROJECT STATUS CHECK
TOOL: ANY
USE WHEN: Starting a session, switching tools, or when you have lost track.
==============================================================================

>>> COPY FROM HERE

Give me a fast situation report on the hackathon project at D:\AGENTX24. This is
ONE continuously evolving project. Inspect only - change nothing, commit nothing,
install nothing.

Inspect:
1. The current project structure (top levels plus the main source areas, skipping
   ignored dependency and build directories).
2. The BUILD[number].md history: list every BUILD*.md, note the highest number,
   read BUILD1.md scope and Core Flow Test plus the newest stage document. Treat
   these as intent, not as proof of what exists.
3. git status
4. git log --oneline -10
5. If it is quick and safe, run the build and start the application or entry
   point to see the actual current run state. If it is not practical, say so and
   report the state as unverified rather than guessing.

Report, briefly:
- CURRENT STAGE: which stage we are on, from the highest BUILD number and the
  commit history. Also state the next BUILD number to be created.
- STATUS: GREEN (builds, runs, core flow works) / YELLOW (runs but something is
  degraded or unverified - name it) / RED (does not build or run) / UNVERIFIED
  (could not check - say why).
- WHAT IS WORKING: features confirmed present in the code, and how you confirmed
  each.
- MOST RECENT CHANGES: the last commits and any uncommitted work in the tree.
- KNOWN PROBLEMS: from the newest Stage Outcome blocks, plus anything you just
  observed.
- DOCUMENT DRIFT: anything a BUILD document claims that the code does not match.
- RECOMMENDED NEXT ACTION: one concrete next step, and which prompt section of
  HACKATHON_PROMPTS.txt to use for it.

Keep it short. Do not propose a plan unless I ask.

<<< COPY TO HERE


==============================================================================
END OF FILE
Reminders: one project, always runnable, verify before claiming, commit locally
after every verified milestone, never commit secrets, freeze new code in the last
20 minutes of every stage.
==============================================================================
